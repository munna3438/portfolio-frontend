# URL of your deployed Express API (Render, Railway, etc.)
# Leave unset locally if you haven't built the backend yet — the
# contact form will just log submissions instead of sending them.
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
